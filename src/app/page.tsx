import { SidebarHeader } from "@/components/SidebarHeader";
import { StackingGallery } from "@/components/sections/StackingGallery";

export default function Home() {
  return (
    <main>
      <SidebarHeader />
      <StackingGallery />
    </main>
  );
}
